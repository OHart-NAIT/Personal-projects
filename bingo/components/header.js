import styles from "@/app/page.module.css";

export default function Header() {
    return (
    // Maybe an image??
    <header className={styles.header}>
        <h1> Bingo Time! </h1> 
    </header>
    
    )   
}